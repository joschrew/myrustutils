[package]
name = "myrustutils"
version = "0.1.1"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow = "1.*"
reqwest = { version = "0.*", default-features = false, features = ["blocking", "multipart"] }
regex = "1.*"
base64 = "0.*"
markdown = "1.0.0-alpha.12"
colored = "2.*"
